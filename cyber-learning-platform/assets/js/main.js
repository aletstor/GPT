document.getElementById('loginForm').addEventListener('submit', function(e) {
  e.preventDefault();
  // Aquí podrías añadir validación o autenticación real
  window.location.href = 'dashboard.html';
});
